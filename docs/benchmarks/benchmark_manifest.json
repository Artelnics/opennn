{
 "schema_version": 2,
 "result_format": "results/README.md",
 "protocol_style": "mlperf_inspired_not_official",
 "description": "Machine-readable inventory of the OpenNN benchmark suite. Each entry names a benchmark's folder, what it compares, the metric names it reports, and the command(s) to run it. No measured results are stored here or anywhere in this tree; runners write their own result JSON under results/. Follow each benchmark's README for full instructions.",
 "benchmarks": [
  {
   "id": "accuracy-higgs",
   "folder": "quality/accuracy",
   "readme": "quality/accuracy/README.md",
   "category": "quality",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, predictive quality parity on the HIGGS dense classifier",
   "metrics": ["test_accuracy", "test_log_loss", "test_roc_auc"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_accuracy",
    "cd docs/benchmarks/quality/accuracy && python run_accuracy.py --engines opennn,pytorch,tensorflow --epochs 5 --runs 5"
   ]
  },
  {
   "id": "precision-rosenbrock",
   "folder": "quality/precision",
   "readme": "quality/precision/README.md",
   "category": "quality",
   "comparison": "OpenNN second-order optimizers (Levenberg-Marquardt, Quasi-Newton) vs PyTorch/TensorFlow first-order optimizers, best error floor on the Rosenbrock regression task (the one documented regression exception to the HIGGS rule)",
   "metrics": ["mse", "wall_time_s"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_precision",
    "cd docs/benchmarks/quality/precision && python run_precision.py"
   ]
  },
  {
   "id": "convergence-higgs",
   "folder": "quality/convergence",
   "readme": "quality/convergence/README.md",
   "category": "quality",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, wall-clock time to a fixed held-out quality target on the HIGGS dense classifier",
   "metrics": ["time_to_target_s", "epochs_to_target", "test_log_loss"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_convergence",
    "cd docs/benchmarks/quality/convergence && python run_convergence.py --target 0.60 --max-epochs 200 --runs 5"
   ]
  },
  {
   "id": "recurrent-lstm-forecasting",
   "folder": "quality/recurrent-lstm-forecasting",
   "readme": "quality/recurrent-lstm-forecasting/README.md",
   "category": "quality",
   "comparison": "OpenNN Recurrent vs OpenNN LSTM on UCI Beijing PM2.5 forecasting (optionally vs PyTorch/TensorFlow), CPU and GPU",
   "metrics": ["test_rmse_mean", "test_rmse_std", "time_s_mean", "cpu_over_gpu"],
   "runner": [
    "cmake --build build-benchmarks --target recurrent_lstm_forecasting_benchmark",
    "cd docs/benchmarks/quality/recurrent-lstm-forecasting && python run_forecasting.py --frameworks opennn,pytorch,tensorflow"
   ]
  },
  {
   "id": "gpu-transformer-inference-speed",
   "folder": "throughput/attention-speed",
   "readme": "throughput/attention-speed/README.md",
   "category": "throughput",
   "comparison": "OpenNN resident Transformer vs PyTorch nn.Transformer vs TensorFlow, encoder-decoder inference throughput, fp32 and bf16",
   "metrics": ["tokens_per_sec", "samples_per_sec"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_transformer_resident opennn_transformer_infer",
    "cd docs/benchmarks/throughput/attention-speed && python run_transformer.py --seqs 128,256,512 --batch 32 --runs 5 --precision both"
   ]
  },
  {
   "id": "gpu-transformer-training-speed",
   "folder": "throughput/attention-speed",
   "readme": "throughput/attention-speed/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, encoder-decoder Transformer training throughput (fwd+bwd+Adam), fp32 and bf16",
   "metrics": ["samples_per_sec", "tokens_per_sec"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_transformer_train",
    "cd docs/benchmarks/throughput/attention-speed && python run_transformer_train.py --runs 5 --precision both"
   ]
  },
  {
   "id": "gpu-resnet50-training-speed",
   "folder": "throughput/resnet50",
   "readme": "throughput/resnet50/README.md",
   "category": "throughput",
   "comparison": "OpenNN CUDA graph vs PyTorch (torch.compile/eager) vs TensorFlow XLA, ResNet-50 training throughput on CIFAR, fp32 and bf16",
   "metrics": ["samples_per_sec", "epoch_s", "speedup"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_resnet50_speed",
    "cd docs/benchmarks/throughput/resnet50 && python run_resnet50.py --dataset cifar10 --epochs 5 --batch 128 --runs 5 --precision both"
   ]
  },
  {
   "id": "gpu-resnet50-inference-speed",
   "folder": "throughput/resnet50",
   "readme": "throughput/resnet50/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, ResNet-50 forward-only inference throughput on CIFAR, fp32 and bf16",
   "metrics": ["samples_per_sec", "ms_per_batch"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_resnet50_infer",
    "cd docs/benchmarks/throughput/resnet50 && python run_resnet50_infer.py --dataset cifar10 --batch 128 --runs 5 --precision both"
   ]
  },
  {
   "id": "gpu-higgs-dense-training-speed",
   "folder": "throughput/higgs-gpu",
   "readme": "throughput/higgs-gpu/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, HIGGS dense training throughput with a test-set quality gate, fp32 and bf16",
   "metrics": ["samples_per_sec", "median_epoch_s", "test_accuracy", "test_log_loss", "test_roc_auc"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_speed",
    "cd docs/benchmarks/throughput/higgs-gpu && python run_higgs_dense.py --train $OPENNN_BENCH_DATA/higgs/higgs_train.csv --test $OPENNN_BENCH_DATA/higgs/higgs_test.csv --epochs 5 --batch 7000 --runs 5 --precision both"
   ]
  },
  {
   "id": "gpu-higgs-dense-inference-speed",
   "folder": "throughput/higgs-gpu",
   "readme": "throughput/higgs-gpu/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, HIGGS dense forward-only inference throughput on GPU, fp32 and bf16",
   "metrics": ["samples_per_sec", "ms_per_batch"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_higgs_infer",
    "cd docs/benchmarks/throughput/higgs-gpu && python run_higgs_infer.py --test $OPENNN_BENCH_DATA/higgs/higgs_test.csv --batch 8192 --runs 5 --precision both"
   ]
  },
  {
   "id": "cpu-higgs-dense-training-speed",
   "folder": "throughput/higgs",
   "readme": "throughput/higgs/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, HIGGS dense training throughput on CPU (fp32), with a test-set quality gate",
   "metrics": ["samples_per_sec", "median_epoch_s", "test_accuracy", "test_log_loss", "test_roc_auc"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_higgs_cpu",
    "cd docs/benchmarks/throughput/higgs && python run_higgs_cpu.py train --train $OPENNN_BENCH_DATA/higgs/higgs_train.csv --test $OPENNN_BENCH_DATA/higgs/higgs_test.csv --epochs 1 --batch 1024 --runs 1"
   ]
  },
  {
   "id": "cpu-higgs-dense-inference-speed",
   "folder": "throughput/higgs",
   "readme": "throughput/higgs/README.md",
   "category": "throughput",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, HIGGS dense forward-only inference throughput on CPU (fp32)",
   "metrics": ["samples_per_sec", "ms_per_batch"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_higgs_cpu",
    "cd docs/benchmarks/throughput/higgs && python run_higgs_cpu.py infer --test $OPENNN_BENCH_DATA/higgs/higgs_test.csv --batch 1024 --runs 5"
   ]
  },
  {
   "id": "gpu-precision-sweep",
   "folder": "throughput/precision-sweep",
   "readme": "throughput/precision-sweep/README.md",
   "category": "throughput",
   "comparison": "OpenNN fp32 vs bf16 speedup across GPU workloads (supporting/internal, not a cross-framework comparison)",
   "metrics": ["samples_per_sec", "tokens_per_sec", "bf16_speedup"],
   "runner": ["cd docs/benchmarks/throughput/precision-sweep && python run_precision_sweep.py --runs 5"]
  },
  {
   "id": "data-capacity",
   "folder": "capacity/data-capacity",
   "readme": "capacity/data-capacity/README.md",
   "category": "capacity",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, most HIGGS tabular rows that fit and train under a fixed RAM cap",
   "metrics": ["max_samples"],
   "runner": ["cd docs/benchmarks/capacity/data-capacity && ./run_sweep.ps1"]
  },
  {
   "id": "gpu-higgs-max-batch",
   "folder": "capacity/higgs-max-batch",
   "readme": "capacity/higgs-max-batch/README.md",
   "category": "capacity",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, largest HIGGS dense batch that completes one step under a memory cap (GPU and CPU, train and infer, fp32 and bf16)",
   "metrics": ["max_batch", "peak_vram_at_max", "next_batch_failed"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_higgs_maxbatch_trial",
    "python docs/benchmarks/capacity/higgs-max-batch/run_higgs_maxbatch.py --engines opennn,pytorch,tensorflow --precisions fp32,bf16 --modes train,infer"
   ]
  },
  {
   "id": "gpu-resnet50-max-batch",
   "folder": "capacity/resnet50-max-batch",
   "readme": "capacity/resnet50-max-batch/README.md",
   "category": "capacity",
   "comparison": "OpenNN ImageDataset BinaryFile path vs PyTorch (torch.compile/eager) vs TensorFlow XLA, largest ResNet-50 training batch that fits",
   "metrics": ["max_training_batch", "peak_vram_mib", "next_failing_batch"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_resnet50_maxbatch_trial",
    "cd docs/benchmarks/capacity/resnet50-max-batch && python run_resnet50_maxbatch.py --dataset cifar10 --precision fp32 --engines opennn,pytorch,tensorflow --start-batch 128"
   ]
  },
  {
   "id": "gpu-transformer-max-batch",
   "folder": "capacity/transformer-max-batch",
   "readme": "capacity/transformer-max-batch/README.md",
   "category": "capacity",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, largest encoder-decoder Transformer batch that fits (train and infer, fp32 and bf16)",
   "metrics": ["max_batch", "speed_samples_per_sec", "peak_vram_at_max"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_transformer_maxbatch_trial",
    "python docs/benchmarks/capacity/transformer-max-batch/prepare_wmt14.py",
    "python docs/benchmarks/capacity/transformer-max-batch/run_transformer_maxbatch.py --engines opennn,pytorch,tensorflow --precisions fp32,bf16 --modes train,infer --corpus $OPENNN_BENCH_DATA/wmt14/wmt14_en_de_pairs.txt"
   ]
  },
  {
   "id": "gpu-transformer-energy-to-target",
   "folder": "energy/transformer-energy",
   "readme": "energy/transformer-energy/README.md",
   "category": "energy",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, GPU energy to train a Transformer to a fixed epoch-mean token cross-entropy target",
   "metrics": ["energy_wh_to_quality_target", "wall_time_to_quality_target", "epochs_to_quality_target"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_transformer_energy",
    "python docs/benchmarks/energy/transformer-energy/run_transformer_energy.py --target 3.5 --batch 128 --lr 1e-4 --runs 4"
   ]
  },
  {
   "id": "application-loc",
   "folder": "footprint/application-loc",
   "readme": "footprint/application-loc/README.md",
   "category": "footprint",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, logical source lines of code for the same Iris workflow",
   "metrics": ["logical_source_lines_of_code"],
   "runner": ["cd docs/benchmarks/footprint/application-loc && python count_lsloc.py"]
  },
  {
   "id": "standalone-code-export",
   "folder": "footprint/export",
   "readme": "footprint/export/README.md",
   "category": "footprint",
   "comparison": "OpenNN source export vs runtime-dependent export paths",
   "metrics": ["export_targets", "runtime_dependency_required"],
   "runner": ["see docs/benchmarks/footprint/export/README.md to build and run the export demo"]
  },
  {
   "id": "baseline-memory-footprint",
   "folder": "footprint/memory",
   "readme": "footprint/memory/README.md",
   "category": "footprint",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, baseline RAM after empty objects and GPU-ready VRAM after one tiny matrix multiply",
   "metrics": ["baseline_ram_mb", "gpu_ready_vram_mb"],
   "runner": [
    "cmake --build build-benchmarks --target opennn_memory",
    "cd docs/benchmarks/footprint/memory && CUDA_MODULE_LOADING=LAZY ./opennn_memory && CUDA_MODULE_LOADING=LAZY python pytorch_memory.py && CUDA_MODULE_LOADING=LAZY TF_FORCE_GPU_ALLOW_GROWTH=true python tensorflow_memory.py"
   ]
  },
  {
   "id": "startup-latency",
   "folder": "footprint/startup",
   "readme": "footprint/startup/README.md",
   "category": "footprint",
   "comparison": "OpenNN vs PyTorch vs TensorFlow, time to first prediction / import-startup overhead",
   "metrics": ["time_to_first_prediction_ms"],
   "runner": ["cd docs/benchmarks/footprint/startup && ./opennn_startup && python pytorch_startup.py && python tensorflow_startup.py"]
  }
 ]
}
