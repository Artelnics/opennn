# Standalone Export Benchmark

Purpose: demonstrate that OpenNN can export a trained model as standalone
source code instead of a runtime-dependent model file.

Top-level note:
[`code-export-opennn-vs-pytorch-vs-tensorflow.md`](code-export-opennn-vs-pytorch-vs-tensorflow.md)

Build and run the local export demo target used by the note, then archive the
generated source and compile/run proof before using the result externally.

Lifecycle: headline candidate after the generated artifact and commands are
archived.
